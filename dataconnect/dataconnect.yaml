specVersion: "v1beta"
serviceId: "fintwin"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "fintwin-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
